# Animal ID & Encyclopedia (Streamlit)

## Features
- Pet Identifier (Qwen-VL via DashScope OpenAI-compatible API)
- Animal Encyclopedia (>= 12 animals per category)
- Multi-language UI (default English)

## Setup
```bash
pip install -r requirements.txt
